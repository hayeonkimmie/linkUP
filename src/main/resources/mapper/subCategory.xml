<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.project">
    <insert id="insertSubCategory" parameterType="subCategory">
        insert subCategory (sub_category_id,category_id,sub_category_name)
        values (#{sub_category_id}, #{category_id},#{sub_category_name})
    </insert>

</mapper>
