<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.qna">

    <select id="selectQnACntByUserId" parameterType="String" resultType="Integer">
        SELECT count(*) FROM qna where user_id = #{userId};
    </select>

    <select id="selectQnAListByPage" parameterType="Map" resultType="qna">
        select *
        from qna
        where user_id = #{userId}
        order by qna_id desc
        limit #{row}, 10
    </select>

    <!--         <mapper resource="mapper/client.xml"/> -->
    <select id="selectQnAList" parameterType="Integer" resultType="qna">
        select * from qna order by answer_date desc limit #{row}, 10
    </select>

    <select id="selectQnACount" resultType="Integer">
        select count(*) from qna

    </select>

    <insert id="insertQna" parameterType="qna">
        insert qna (qna_id,user_id,question_title,question_content,question_date,answer_content,answer_date)
        values (#{qna_id}, #{user_id}, #{question_title},#{question_content},#{question_date},#{answer_content},#{answer_date})
    </insert>
</mapper>
