<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.settlement">
    <!-- 4. 정산 내역 테이블 등록 -->
    <insert id="insertSettlement" parameterType="dto.Settlement" useGeneratedKeys="true">
        INSERT INTO settlement (
            slist_id,
            category_id,
            client_id,
            pname,
            ammount,
            start_date,
            end_date,
            settleday,
            position,
            name,
            status,
            account
        ) VALUES (
                     #{slistId},
                     #{categoryId},
                     #{clientId},
                     #{pname},
                     #{ammount},
                     #{startDate},
                     #{endDate},
                     #{settleday},
                     #{position},
                     #{name},
                     #{status},
                     #{account}
                 )
    </insert>

    <select id="countSettlementsByProjectAndDate" parameterType="Map" resultType="int">
        SELECT COUNT(*)
        FROM settlement s
                 JOIN settlelist sl ON s.slist_id = sl.slist_id
        WHERE sl.project_id = #{projectId}
          AND sl.settle_date = #{settleDate}
    </select>

    <select id="existsSettlementBySlistIdAndsettleDate" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM settlement
        WHERE slist_id = #{slistId}
          AND settleday = #{settleDate}
    </select>


</mapper>
