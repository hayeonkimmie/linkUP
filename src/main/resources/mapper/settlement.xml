<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.settlement">
    <!-- 4. 정산 내역 테이블 등록 -->
    <insert id="insertSettlement" parameterType="settlement">
        INSERT INTO settlement (slist_id, category_id, client_id, pname, pay, start_date, end_date, settleday, position, name, status, account)
        VALUES (#{slistId}, #{categoryId}, #{clientId}, #{pname}, #{pay}, #{startDate}, #{endDate}, #{settleday}, #{position}, #{name}, #{status}, #{account})
    </insert>


</mapper>
